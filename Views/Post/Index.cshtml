@model UserPostRatingViewModel
@{
    ViewBag.Title = "PostIndex";
    Layout = "_Layout";
}

<h2>All Posts (where public)</h2>
@foreach (var post in Model.Posts)
{
    <p>@post.Id - @post.Title</p>
}