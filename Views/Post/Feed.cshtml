@model UserPostRatingViewModel
@{
    ViewBag.Title = "Feed";
    Layout = "_Layout";
}

<div class="w-full">
    <h1 class="text-4xl font-bold text-gray-900 dark:text-white items-center place-self-start">Feed</h1>
</div>

<div class="w-full my-6">
    <div class="w-full flex justify-center">
        <a class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
           asp-controller="Post" asp-action="Create">Create Post</a>
    </div>
</div>

@Html.Partial("_PostList")